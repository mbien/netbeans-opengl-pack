application.args=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/jogl-project.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.gluegen-rt.jar=lib/gluegen-rt.jar
file.reference.jogl.all.jar=lib/jogl.all.jar
file.reference.nativewindow.all.jar=lib/nativewindow.all.jar
file.reference.newt.all.jar=lib/newt.all.jar
includes=**
jar.compress=false
javac.classpath=\
    ${file.reference.jogl.all.jar}:\
    ${file.reference.gluegen-rt.jar}:\
    ${file.reference.nativewindow.all.jar}:\
    ${file.reference.newt.all.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=jogl-project.Main
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
#platform to run this project on; supported values:
# linux-i586
# linux-amd64
# macosx-ppc
# macosx-universal
# solaris-sparc
# solaris-sparcv9
# solaris-i586
# windows-i586
# windows-amd64
natives.platform=natives.platform
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=-Djava.library.path="lib"
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=src
test.src.dir=test
